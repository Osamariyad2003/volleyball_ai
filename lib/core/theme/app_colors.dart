import 'package:flutter/material.dart';

class AppColors {
  // Brand colors derived from seed or defined explicitly
  static const Color primarySeed = Color(0xFF6750A4); // Classic Material 3 primary seed
  
  // Custom colors if needed for duotone/premium look
  static const Color background = Color(0xFF1C1B1F); // Material 3 Dark Background
  static const Color surface = Color(0xFF1C1B1F);
  static const Color primary = Color(0xFFD0BCFF);
  
  // Duotone support colors
  static const Color duotonePrimary = Color(0xFFD0BCFF);
  static const Color duotoneSecondary = Color(0xFFCCC2DC);
}
